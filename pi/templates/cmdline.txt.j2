# Kernel command line for NFS boot for Raspberry Pi {{ client_hostname }}

console=serial0,115200 console=tty1 root=/dev/nfs nfsroot={{ nfs_server_ip }}:{{ client_nfs_rootfs_path }},vers=4.1,proto=tcp rw ip=dhcp rootwait elevator=deadline
